{
  "name": "wepi-monorepo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm --filter wepi test",
    "dev": "pnpm --filter wepi-client dev"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
